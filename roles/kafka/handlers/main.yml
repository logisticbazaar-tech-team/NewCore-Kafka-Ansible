---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart Kafka
  ansible.builtin.systemd_service:
    name: kafka
    state: restarted
  when: not ansible_check_mode
